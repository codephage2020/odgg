// Saved datasource configuration
export interface SavedDatasource {
  id: string;
  name: string;
  host: string;
  port: string;
  database: string;
  username: string;
  schema: string;
  dbType: string;
  createdAt: string;
  lastUsedAt: string;
}
